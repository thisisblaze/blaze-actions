name: "Test One Input"
on:
  workflow_call:
    inputs:
      env_name:
        required: true
        type: string
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ inputs.env_name }}"

name: "✅ DNS Verify & Activate (Phase C)"

on:
  workflow_call:
    inputs:
      cloud_provider:
        description: "Cloud provider (aws/gcp/azure). Default: aws"
        required: false
        type: string
        default: "aws"
      tf_dir:
        description: "Directory containing Terraform configuration"
        required: true
        type: string
      aws_region:
        description: "AWS Region"
        required: true
        type: string
      environment:
        description: "GitHub Environment (for secrets)"
        required: false
        type: string
      bucket:
        description: "S3 Bucket for State"
        required: true
        type: string
      state_key:
        description: "S3 Key for State"
        required: true
        type: string
      lock_table:
        description: "DynamoDB Table for Locking"
        required: false
        type: string
      terraform_version:
        description: "Terraform Version"
        required: false
        type: string
        default: "1.9.0"
      pre_apply_script:
        description: "Bash script to run after Init but before Plan/Apply"
        required: false
        type: string
      wif_audience:
        description: "WIF Audience (Optional)"
        required: false
        type: string

jobs:
  verify:
    uses: ./.github/workflows/reusable-terraform.yml
    with:
      cloud_provider: ${{ inputs.cloud_provider }}
      tf_dir: ${{ inputs.tf_dir }}
      aws_region: ${{ inputs.aws_region }}
      environment: ${{ inputs.environment }}
      bucket: ${{ inputs.bucket }}
      state_key: ${{ inputs.state_key }}
      lock_table: ${{ inputs.lock_table }}
      terraform_version: ${{ inputs.terraform_version }}
      pre_apply_script: ${{ inputs.pre_apply_script }}
      wif_audience: ${{ inputs.wif_audience }}
      apply: true
      # Phase C: Verify cert/records and bind
      tf_vars: '{"manage_dns":false,"await_validation":true}'
